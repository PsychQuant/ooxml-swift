// Phase 7 placeholder.
// Full type design lives in `openspec/specs/mdocx-grammar/spec.md`
// (Spectra change `mdocx-syntax`). Implementation is the responsibility of
// `word-aligned-state-sync` Phase 7.

/// Root container of an `.mdocx` script. `WordDocument` is the top-level
/// result builder entry point; its body is a sequence of `Section` containers.
public struct WordDocument {
    public init() {}
}
